# TTALGAK 창던지기 v3

Native macOS 13+ menu-bar mini game. `I_L` is the only pointer input: a transparent `180 × 110 pt` nonactivating panel, placed `96pt` inward from the physical left edge. Pointer down must begin in its visible actor/held-spear `44 × 44 pt` local zone, centered on the launch hand at local `(66,58)`; longer reverse pulls intentionally leave `I_L` and stay valid because local drag/up containment is ignored after a valid down. `I_R` remains a transparent `180 × 110 pt` display-only structural panel. The actor, target, result, persisted ground spears, and flight render on a separate full-primary-display transparent display panel; it ignores mouse events and is never key/main. The physical `groundY` plane is intentionally invisible: it is used only for first-crossing miss resolution, never as a rendered ground line.

## v3 contract

- Local vertical drag maps exactly to an asymmetric `10…65°` range around the `45°` center in y-up points: `angle=clamp(45 + 35×dy/48, 10, 65)` for `dy<0` and `angle=clamp(45 + 20×dy/48, 10, 65)` for `dy≥0`, so `dy=-48 → 10°`, `dy=0 → 45°`, `dy=+48 → 65°`. Reverse tangent projection maps power with a 6pt dead zone and a dynamic screen-edge maximum: `r=max(0,-dot(D,t))`, `power=clamp((r-6)/(maxPull-6),0,1)`.
- Dynamic screen-edge max pull: at the valid actor-zone `mouseDown`, the press point converts to screen coordinates only through the local `NSView → NSWindow → convertToScreen` chain (no global pointer query). At the first 6pt dead-zone crossing, `maxPull` freezes together with the tangent as the slab-exit ray distance from that screen press point along the frozen reverse direction (`-tangent`) to the FIRST primary-screen boundary (`NSScreen.main.frame`; usually left/bottom for right/up aims). The pointer reaching that display edge is exactly `power=1`; a start on/past an edge, or a `maxPull ≤ 6pt`, fails closed to zero power — it is never remapped to any fixed pull constant.
- Screen-edge recovery corridor: only after that first valid reverse dead-zone crossing, the app opens an ephemeral union of overlapping transparent nonactivating `32×32pt` local panels from the press point along the frozen reverse ray through its first primary-screen boundary. The union has a documented conservative outer width of `46pt` around any ray; tiles are clipped to `NSScreen.main.frame`, and no fullscreen input window exists. Therefore empty desktop outside the tile union has no TTALGAK input window and remains pass-through. The corridor only forwards its own local `mouseDragged`/`mouseUp` coordinates through `NSWindow.convertToScreen`; it neither starts nor changes the latch, and it closes on release, zero-power release, cancel, hide/toggle, flight resolution, or reset. There is no event monitor/tap, global pointer query, cursor warp, capture, or permission API.
- Power latch: the moment a valid drag first crosses the 6pt reverse dead zone, the current angle's tangent and its screen-edge `maxPull` are frozen. From then on power reads only the displacement component along that frozen axis (`r=-dx/tangent.x`, `clamp((r-6)/(maxPull-6),0,1)`): vertical angle-only moves change the launch angle but leave power, raw pull, and `maxPull` exactly unchanged; only continued pull along the frozen reverse axis raises (or relaxes) power, reaching max exactly at the screen edge. Before the dead zone is crossed, angle changes never latch power. `mouseUp` launches the latest angle with the latched power.
- Visible pull: the frozen launch retains the raw frozen-axis pull distance (`rawPull`) and the frozen `maxPull` separately from normalized power, and the tension line renders them proportionally: `tension = clamp(rawPull/maxPull × 160, 0, 160)pt`, hidden inside the 6pt dead zone. A full screen-edge pull shows the 160pt tension line and a half-edge pull shows 80pt. Vertical angle-only moves preserve raw pull, `maxPull`, visible tension, and power together; only further pull along the frozen reverse axis grows them. The hand/launch `P0` never moves during aiming, so tension is the only visual pull indicator and flight starts with no hand-to-spear discontinuity.
- Down/move/up are local NSView events only. A down must begin in the actor zone; after the latch, either the original input view or a bounded corridor tile may deliver local drag/up coordinates, and the first actual `mouseUp` launches the final frozen value once. The next valid down discards stale aim. There are no event monitors, taps, global pointer/key/cursor APIs, capture, network, clipboard, or permission APIs.
- Pure launch input is frozen `(P0, angle, power, groundY)`. `g=2400 pt/s²`, `v0=900+1000×power`, `L=42`; target seed/result never participate in velocity or trajectory calculation.
- Hit is only physical `R=16` shaft-segment/ring collision. Miss is the first physical ground-plane crossing, or an honest off-display miss. No target steering, canonical target calibration, endpoint blend, prediction arc, numeric gauge, card, or angle label exists.
- Ground-miss spears persist where they crossed the hidden ground plane, drawn with the same black stroke language. The inventory is an in-memory FIFO capped at 50: the 51st miss evicts the oldest. Hit spears are transient and never enter the inventory. Nothing is persisted to disk or network.
- Target spawns only at show/new-game and after an actual hit, at `x = screen.maxX − inset` with inset uniformly in `280…520pt` (clamped to visible bounds by a 22pt edge margin), vertical seed heights `ground+64/132/200` cycling deterministically via an injectable provider. Misses, resets, and flight never move the target, and the target never influences launch velocity or physics.
- Standard mode uses common-run-loop monotonic elapsed time and ≤4pt collision substeps. Reduced Motion evaluates the same frozen physics immediately, shows a static truthful result, and creates no flight trajectory layer.

Existing internal PNG/SVG assets are not v3 final character-quality acceptance; the code-drawn actor is an intentionally safe fallback.

## Linux static validation

```bash
python3 Tests/verify_project.py
python3 -m py_compile Tests/verify_project.py Tools/regenerate_motion_pngs.py
```

Linux has no Swift toolchain in this environment, so these checks do not claim Swift/AppKit compilation or runtime success.

## Mandatory Mac QA handoff

```bash
cd /Users/littleduck/Documents/GitHub/TTALGAK
git pull --ff-only origin main
swift package describe
swift test
swift build
swift run TTALGAK
```

Run the M-V3 matrix after the four commands pass: same 45° short/mid/long reverse pulls must have strictly rising landing distance; 10°/45°/65° at equal power must differ; drag fully down must reach the new 10° minimum (no drag = 45°, fully up = 65°), and a 10° full-power throw must produce a fast, flat, honestly landing low trajectory with its ground-miss spear persisting in the scene; actual ground misses, non-clipping actor/target, focus/pass-through/non-key behavior, reduced-motion truth, timer cleanup, and zero permission prompts must be recorded PASS/FAIL. Additionally: fire 51 ground misses and confirm only 50 spears remain with the oldest removed; confirm the target stays exactly still across misses/resets and moves only after an actual hit; confirm every spawned target x sits within `280…520pt` inset from the right screen edge; confirm maximum power now requires pulling the cursor all the way to the physical display edge in the frozen reverse-pull direction (usually the left/bottom edge): only when the cursor touches that screen boundary may the tension line reach its full `160pt` and the throw reach full power, and stopping short of the edge must read proportionally less. Power-latch check: pull back past the dead zone to establish visible tension, then move only vertically — tension/power must not shrink or grow while the aim angle changes (the frozen screen-edge maximum must not change either); only further backward pull along the latched direction may change it, and release must launch with the latest angle plus the latched power. Visible-pull check: pull halfway to the screen edge and confirm the tension line reads ≈`80pt`, then continue until the cursor touches the edge and confirm it reads the full `160pt`; a vertical-only angle adjustment at any held pull must not visibly shrink or grow the tension line, and the hand/spear origin must stay fixed through release into flight. Corridor Mac QA (pending): actor down, drag exactly inside the frozen corridor to the first screen edge, and release there must launch exactly once; an empty desktop point outside the corridor must remain pass-through; the corridor must disappear after release, cancel, and hide; no Accessibility/Input Monitoring/Screen Recording prompt may appear; the scene actor and target must remain visible throughout. v3 is a candidate implementation until real Mac QA passes.
